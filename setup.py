from setuptools import setup, find_packages

setup(
    name='bitscrunch_sdk',
    version='0.1.0',
    description='A Python SDK for interacting with the bitsCrunch API',
    author='Your Name',
    author_email='your.email@example.com',
    packages=find_packages(),
    install_requires=[
        'requests'
    ],
)
